# WordsAI - 部署操作指南

## 🖥️ 正在进行的操作

### 1. Vercel 登录
已自动打开浏览器访问 Vercel 登录页面

**操作步骤**:
1. 在打开的浏览器中登录/注册 Vercel 账号
2. 登录成功后，回到命令行

### 2. 自动部署脚本
登录后，双击运行以下文件：
```
C:\Users\24246\.claude\ai-project\with-supabase-app\auto-deploy.bat
```

脚本会自动：
1. 构建项目
2. 检查登录状态
3. 部署到 Vercel
4. 返回部署 URL

---

## 📱 Android APK 构建

### 方式一：使用 Android Studio (推荐)

1. **打开 Android Studio 项目**
   - 双击运行: `update-config.bat`
   - 或执行: `npm run cap:open:android`

2. **等待 Gradle 同步**
   - 首次打开需要下载依赖，可能需要 5-15 分钟

3. **构建 APK**
   - 点击菜单: Build → Build Bundle(s) / APK(s) → Build APK(s)
   - 等待构建完成

4. **获取 APK**
   - 构建完成后点击弹出的通知
   - 或手动打开: `android\app\build\outputs\apk\debug\app-debug.apk`

### 方式二：命令行构建 (需要 JDK)

1. **安装 JDK 17**
   - 下载: https://adoptium.net/temurin/releases/?version=17
   - 安装到默认路径

2. **构建**
   ```bash
   cd android
   gradlew.bat assembleDebug
   ```

---

## 🔧 简化部署命令

### 已配置的 npm 脚本

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 构建生产版本 |
| `npm run deploy:vercel` | 部署到 Vercel |
| `npm run deploy:android` | 同步 Android 项目 |
| `npm run cap:open:android` | 打开 Android Studio |

---

## 📋 部署流程图

```
1. Vercel 登录 (浏览器中完成)
       ↓
2. 运行 auto-deploy.bat
       ↓
3. 获取部署 URL (如: https://wordsai-xxx.vercel.app)
       ↓
4. 运行 update-config.bat 更新配置
       ↓
5. 打开 Android Studio 构建 APK
       ↓
6. 安装 APK 到手机测试
       ↓
7. 功能正常 → 发布到应用商店
```

---

## ⚠️ 常见问题

### Q: Vercel 登录失败?
A: 使用浏览器直接访问 https://vercel.com/login，登录后返回命令行

### Q: 部署后应用无法连接?
A: 检查 `capacitor.config.ts` 中的 URL 是否正确

### Q: Android Studio Gradle 同步失败?
A: 检查网络，可能需要配置代理或等待更长时间

### Q: APK 安装失败?
A: 确保手机开启了"允许安装未知来源应用"

---

## 📞 需要帮助?

1. **查看文档**
   - `PROJECT_SUMMARY.md` - 项目总结
   - `DEPLOYMENT.md` - 详细部署指南
   - `QUICK_DEPLOY.md` - 快速部署指南

2. **运行脚本**
   - `auto-deploy.bat` - 自动部署
   - `update-config.bat` - 更新配置
